"use strict"

var EvetEmitter = require('events').EvetEmitter;

// TODO: declare the data model
// TODO: make the database accessible only on the server

class Appointment extends EvetEmitter {
  getDatesAfter(date) {

  }
});




module.exports = Appointment;