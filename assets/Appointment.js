/* @flow */

"use strict"

var EventEmitter = require('eventemitter2');
var Dispatcher = require('flux').Dispatcher;

// TODO: declare the data model
// TODO: make the database accessible only on the server
// TODO: configure the Flux dispatcher for this store
// TODO: create an action for appointments

  
class Appointment extends EventEmitter {
  getDatesAfter(date: string) {

  }
};




module.exports = Appointment;